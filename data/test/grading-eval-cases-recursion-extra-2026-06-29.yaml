- id: grading_eval_bad_08
  label: bad
  expected_min: 0
  expected_max: 40
  content: |
    Recursion just means repeating the same function again and again.
    You do not need a base case because the computer will stop automatically.

- id: grading_eval_bad_09
  label: bad
  expected_min: 0
  expected_max: 42
  content: |
    A recursive solution is basically the same as a loop.
    The deeper the calls go, the more complete the answer is, so stopping conditions are not important.

- id: grading_eval_bad_10
  label: bad
  expected_min: 0
  expected_max: 45
  content: |
    Recursion means a function calls itself.
    I am not sure what a base case is, but I think the system will eventually return by itself.

- id: grading_eval_bad_11
  label: bad
  expected_min: 0
  expected_max: 45
  content: |
    In factorial problems, n does not really need to get smaller.
    The function can keep calling itself and still get the result later.

- id: grading_eval_mid_08
  label: mid
  expected_min: 58
  expected_max: 80
  content: |
    Recursion is a way for a function to solve a problem by calling itself on a smaller version.
    A recursive design should include a termination condition.
    For example, factorial uses factorial(0)=1 and otherwise calls factorial(n-1).
    However, this answer does not clearly explain why missing the base case causes failure.

- id: grading_eval_mid_09
  label: mid
  expected_min: 60
  expected_max: 82
  content: |
    A recursive function usually has two parts: the stopping condition and the recursive step.
    In factorial, n keeps moving toward 0, so the process can end.
    This explanation is correct in the main idea, but it is still brief and not very systematic.

- id: grading_eval_mid_10
  label: mid
  expected_min: 60
  expected_max: 82
  content: |
    Recursion breaks one big problem into smaller problems of the same type.
    The function calls itself and should contain a base case.
    Fibonacci is a common example, but this answer does not clearly distinguish the recursive case from the base case.

- id: grading_eval_mid_11
  label: mid
  expected_min: 62
  expected_max: 82
  content: |
    Recursion is common in programming because a function can use itself to solve a related subproblem.
    You should make sure there is a termination condition and that the input moves toward that condition.
    This answer is basically correct, but it does not give a full worked example.

- id: grading_eval_good_08
  label: good
  expected_min: 84
  expected_max: 100
  content: |
    Recursion solves a problem by reducing it to a smaller subproblem of the same kind.
    A complete recursive definition needs a base case and a recursive case.
    For example, factorial(0)=1 is the base case, and factorial(n)=n*factorial(n-1) is the recursive case.
    Because n decreases each time, the calls converge to the base case; otherwise the program may recurse forever and overflow the stack.

- id: grading_eval_good_09
  label: good
  expected_min: 85
  expected_max: 100
  content: |
    The key idea of recursion is not simply that a function calls itself, but that each call solves a smaller instance of the same problem.
    A sound recursive design must specify a base case to stop and a recursive step to reduce problem size.
    In tree traversal, for example, reaching an empty node is the base case, and visiting child nodes is the recursive step.
    Without a stopping condition or size reduction, recursion will not converge.

- id: grading_eval_good_10
  label: good
  expected_min: 84
  expected_max: 100
  content: |
    A high-quality explanation of recursion should include the definition, the base case, the recursive case, and the reason the parameters approach termination.
    In factorial, the base case is n==0 returning 1, and the recursive case is n*factorial(n-1).
    Since n becomes smaller on each call, the computation eventually reaches the base case and returns safely.

- id: grading_eval_good_11
  label: good
  expected_min: 86
  expected_max: 100
  content: |
    Recursion is a problem-solving method in which a function handles a problem by delegating a smaller problem of the same form to itself.
    To be correct, the definition must contain a base case, a recursive case, and a guarantee that each step moves toward the base case.
    Hanoi Tower, factorial, and tree traversal all follow this structure.
    If the base case is missing or the problem size does not shrink, the recursion may never terminate.
