2026 Data Analysis and Mining - Homework 03 

You   are   to   implement   both   algorithms without   using   high-level   machine   learning
libraries (e.g., sklearn.cluster). You may use NumPy for efficient matrix operations.
Requirements:
 Hierarchical Clustering (my_hierarchical):

Input: Data matrix (n × d), number of clusters (k), linkage type.

o
o Output:  Cluster   labels   for   each   point,   a   basic   linkage   matrix   (to   support   dendrogram

visualization).
Implement at least two linkage criteria:

o

Single Linkage :d (C i ,C j)=min {d ( x , y ): x ∈ Ci , y ∈ C j}

 Complete Linkage : d (C i ,C j)=m ax {d ( x , y ): x ∈C i, y ∈C j }



o Optimization: Use a distance matrix to avoid redundant calculations.
Spectral Clustering (my_spectral):
o

Input:Data matrix (n × d), number of clusters (k),affinity parameter (σ ¿

o Output: Cluster labels for each point.
o Procedure:

1. Construct Similarity Graph ( W ):Use a Fully Connected graph with RBF Kernel

2. Compute Degree Matrix D.
3. Compute the Normalized Symmetric Laplacian:

Lsym=I − D− 1/ 2W D−1 /2.

4. Eigenvalue   Decomposition:   Find   the  k   eigenvectors   corresponding   to   the  k

smallest eigenvalues of Lsym.

5. Normalization: Normalize rows of U to get final labels.
6. Final Clustering: Run K-Means on the rows of U to obtain final labels.

least

three   datasets from  

Select at
the   UCI   Machine   Learning   Repository
(https://archive.ics.uci.edu/)  (or   equivalent).   Choose   datasets   of   varying   sizes   and  dimensions.
Examples:

Iris (150 samples, 4 dimensions, 3 classes)

 Wine (178 samples, 13 dimensions, 3 classes)
 MNIST (small subset) or Optdigits (e.g., 1000 samples, 64 dimensions, 10 classes)



Synthetic dataset (you can generate to test scalability)

For each dataset, perform the following:

1. Preprocessing: Standardize features to zero mean and unit variance.
2. Parameter Setting: Use the true number of clusters (if known) or determine a reasonable

k via elbow method.

3. Experiments:

Hierarchical Clustering:

 
 
o Run   HAC   with   both   Single   and   Complete   linkage   (once   each,   deterministic).

Record:

Silhouette Score


 Adjusted Rand Index (ARI)
 Runtime
Plot the dendrogram for one dataset (e.g., Iris or Seeds)

o
Spectral Clustering:
o Run 5–10 times for each parameter setting (due to randomness in the final K-Means

step)

o Experiment with different σvalues (e.g., 0.1, 0.5, 1, 2, 5) or different kNN neighbor

counts

o Record:

 Average Silhouette Score 
 Average ARI 
 Average Runtime
For one dataset, measure the scalability:

o

 Vary   the   number   of   samples   (e.g.,   100,   500,   1000,   2000,   5000)   and   plot

runtime vs. data size.

 Vary   the   number   of   dimensions   (e.g.,   use   PCA   to   reduce   or   increase
dimensions   artificially,   or   use   subsets   of   features)   and   plot   runtime   vs.
dimensions.

4.  Comparison:

Compare the following algorithms:

 Hierarchical Clustering (Complete linkage)

Spectral Clustering (best parameter)


 K-Means (as a baseline)

1. Algorithm   Description: Briefly   explain   how   Hierarchical   Clustering   and   Spectral

Clustering implementations work. Highlight any optimizations.

2. Experimental Setup: List datasets, preprocessing steps, and parameter choices.
3. Results:

o Tables showing Silhouette Score (mean ± std), ARI (mean ± std), and runtime for

each algorithm

o Graphs for scalability experiments (runtime vs. sample size)
o Visualizations 

 Dendrogram for Hierarchical Clustering



Scatter plots of clustering results for non-convex data (K-Means vs. Spectral
vs. Hierarchical)

4. Discussion:

o Why   does   K-Means   fail   on   non-convex   datasets   while   Spectral   Clustering

succeeds?

o How does the linkage criteria (Single vs. Complete) affect sensitivity to noise?
o What   are   the   trade-offs  (quality   vs.   speed   vs.   interpretability)   among   the   three

algorithms?

o Any observed limitations or failure cases?

Submission Requirements 
It   is   strictly   required   that   the   assignment   be   submitted   as  a   ZIP   file  named

studentID_name_hw3.zip. 

The file should include the following contents: 
1. Source Code:

o

o

o

o

spectral.py – Implementation of Spectral Clustering.
hierarchical.py – Implementation of Hierarchical Clustering.
experiments.py – Script that runs all experiments and generates results.
requirements.txt –   List   of   libraries   used   (e.g.,   numpy,   matplotlib,   scikit-learn   for

metrics only, not clustering).

o Note: Your code must be well-commented and organized.

2. Report (PDF): The analysis as described in Section 3.3.
3. README: Instructions on how to run your code and reproduce the experiments.
Submission Deadline: 2026/04/17, 23:59:59. 
Note: 
The datasets required to complete the task is available on Canvas. 
Please submit your file to Canvas.

